<script>
  import { onMount } from 'svelte';
  
  // Import CSS files that will be loaded when this component is used
  import '$lib/styles/theme.css';
  import '$lib/styles/leaderboard.css';
  
  // This component doesn't render anything, it just loads the CSS
</script>

<!-- This component doesn't render anything -->
